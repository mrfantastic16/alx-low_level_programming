#include <stdio.h>

/**
 * main - printing the sizeof of an array
 * 
 * Retturn: Always 0
*/

int main(void)
{
    int array[10];

    printf("sizeof array: %lu\n", sizeof(array));

    return (0);
}