#include <stdio.h>

/**
 * main - prints a str to stdout using printf
 * Return: returns 0 on succes
*/

int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
