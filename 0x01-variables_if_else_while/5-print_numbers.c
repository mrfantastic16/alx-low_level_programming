#include <stdio.h>

/**
 * main - prints all single digits of base 10
 *
 * Returns: 0 on success
 */

int main(void)
{
	int i;
	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
